server:
  port: 9999
  
zuul:
  ignored-services: "*"
  routes:
    CargoTrackerService:
      path: /tracker/**
      serviceId: Cargotracker
      stripPrefix: true
    PathfinderService: 
      path: /pathfinder/**
      serviceId: Pathfinder
      stripPrefix: true

hystrix:
  command:
    default:
      execution:
        timeout:
          enabled: false 
